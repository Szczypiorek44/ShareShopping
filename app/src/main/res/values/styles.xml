<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Design.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/grey500</item>
        <item name="colorAccent">@color/grey900</item>
        <item name="android:windowBackground">@color/grey200</item>
        <item name="buttonStyle">@style/Base.Widget.AppCompat.Button.Borderless</item>
    </style>

</resources>
